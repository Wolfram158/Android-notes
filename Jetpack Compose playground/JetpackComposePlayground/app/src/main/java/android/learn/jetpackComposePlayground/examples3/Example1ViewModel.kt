package android.learn.jetpackComposePlayground.examples3

import androidx.lifecycle.ViewModel

class Example1ViewModel : ViewModel() {
    private val dataSource = mutableListOf<Int>().apply {

    }
}